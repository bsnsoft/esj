/**
 * The generator of the artefacts that follow from the term registry: the sources of the
 * typed view and the model schema.
 *
 * <p>It is a build tool and no module depends on it. Its output is a function of the
 * registry alone, so the build can check the checked-in files against it; see
 * {@link de.bsnsoft.esj.generator.Generate}.
 */
package de.bsnsoft.esj.generator;
