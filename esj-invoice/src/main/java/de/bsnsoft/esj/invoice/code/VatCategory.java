// Generated by esj-generator from the code list snapshots
// of the rule pack en16931 1.3.16: untdid-5305 of 2026-09-19.
// Do not edit.
package de.bsnsoft.esj.invoice.code;

import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;

/**
 * VAT category of a line, an allowance, a charge or a breakdown.
 *
 * <p>UNTDID 5305 duty or tax or fee category code, as admitted by EN 16931. UN/CEFACT (UNTDID),
 * listed for EN 16931 by the European Commission, snapshot of 2026-09-19, 9 codes.
 *
 * <p>The registry names this list at BT-95, BT-102, BT-118, BT-151.
 *
 * <p>A code the snapshot does not carry is representable: {@code VatCategory.custom(code)} yields a
 * {@link CustomCode}, and {@code resolve(code)} yields the constant where there is one and a {@code
 * CustomCode} where there is none. Whether a code is admissible is decided by the rule pack and not
 * here.
 */
public enum VatCategory implements Coded {

    /** {@code S}, Standard rate. */
    STANDARD("S", "Standard rate", "untdid-5305"),
    /** {@code Z}, Zero rated goods. */
    ZERO_RATED("Z", "Zero rated goods", "untdid-5305"),
    /** {@code E}, Exempt from tax. */
    EXEMPT("E", "Exempt from tax", "untdid-5305"),
    /** {@code AE}, VAT Reverse charge. */
    REVERSE_CHARGE("AE", "VAT Reverse charge", "untdid-5305"),
    /** {@code K}, VAT exempt for EEA intra-community supply of goods and services. */
    INTRA_COMMUNITY("K", "VAT exempt for EEA intra-community supply of goods and services", "untdid-5305"),
    /** {@code G}, Free export item, tax not charged. */
    EXPORT("G", "Free export item, tax not charged", "untdid-5305"),
    /** {@code O}, Service outside scope of tax. */
    OUTSIDE_SCOPE("O", "Service outside scope of tax", "untdid-5305"),
    /** {@code L}, Canary Islands general indirect tax. */
    CANARY_ISLANDS("L", "Canary Islands general indirect tax", "untdid-5305"),
    /** {@code M}, Tax for production, services and importation in Ceuta and Melilla. */
    CEUTA_AND_MELILLA("M", "Tax for production, services and importation in Ceuta and Melilla", "untdid-5305");

    private static final Map<String, VatCategory> BY_CODE = byCode();

    private final String code;

    private final String publishedName;

    private final String listId;

    VatCategory(String code, String publishedName, String listId) {
        this.code = code;
        this.publishedName = publishedName;
        this.listId = listId;
    }

    /**
     * Returns the constant carrying a code.
     *
     * <p>The comparison is exact, code point by code point: a code list is a list of
     * codes and not of their spellings.
     *
     * @param code the code
     * @return the constant
     * @throws IllegalArgumentException if the snapshot does not carry the code
     * @throws NullPointerException     if {@code code} is {@code null}
     */
    public static VatCategory of(String code) {
        VatCategory value = BY_CODE.get(Objects.requireNonNull(code, "code"));
        if (value == null) {
            throw new IllegalArgumentException("the snapshot of VatCategory does not carry the code "
                    + code + "; VatCategory.custom(code) represents a code it does not have");
        }
        return value;
    }

    /**
     * Returns the constant carrying a code, or a custom code where there is none.
     *
     * @param code the code as a document carries it
     * @return the constant or a {@link CustomCode}
     * @throws NullPointerException     if {@code code} is {@code null}
     * @throws IllegalArgumentException if the code is blank
     */
    public static Coded resolve(String code) {
        VatCategory value = BY_CODE.get(Objects.requireNonNull(code, "code"));
        return value != null ? value : new CustomCode("VatCategory", code);
    }

    /**
     * Returns a code the snapshot does not carry.
     *
     * @param code the code
     * @return the custom code
     * @throws IllegalArgumentException if the snapshot carries the code, which has a
     *                                  constant and must not have a second representation,
     *                                  or if the code is blank
     * @throws NullPointerException     if {@code code} is {@code null}
     */
    public static Coded custom(String code) {
        VatCategory value = BY_CODE.get(Objects.requireNonNull(code, "code"));
        if (value != null) {
            throw new IllegalArgumentException("the snapshot of VatCategory carries the code "
                    + code + " as " + value.name());
        }
        return new CustomCode("VatCategory", code);
    }

    /**
     * Returns the code as it is written into the document.
     *
     * @return the code
     */
    @Override
    public String code() {
        return code;
    }

    /**
     * Returns the name the publisher of the list gives the code.
     *
     * @return the name, empty where the publisher gives none
     */
    @Override
    public String publishedName() {
        return publishedName;
    }

    /**
     * Returns the identifier of the code list the constant came from.
     *
     * @return the list identifier, for example {@code untdid-5305}
     */
    @Override
    public String listId() {
        return listId;
    }

    private static Map<String, VatCategory> byCode() {
        Map<String, VatCategory> byCode = new LinkedHashMap<>();
        for (VatCategory value : values()) {
            byCode.put(value.code, value);
        }
        return Map.copyOf(byCode);
    }
}
