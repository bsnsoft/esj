// Generated by esj-generator from EN16931 registry
// EN 16931-1:2017+A1:2019/AC:2020 with ESJ-B2C 0.1, registry version 0.1.
// Do not edit.

package de.bsnsoft.esj.b2c;

import de.bsnsoft.esj.SemanticDocument;
import de.bsnsoft.esj.SemanticPath;
import java.math.BigDecimal;

/**
 * Writes the terms of ESJ-B2C 0.1 that lie inside BG-25 INVOICE LINE.
 *
 * <p>An overlay over the same document builder the typed editor of the core model writes into,
 * carrying the terms of the extension and nothing else. Setting null or an empty string removes the
 * value, exactly as in the core editor. A group comes into being when a value is written into it
 * (specification, section 4.5); this overlay appends no instance of a core group and removes none.
 */
public interface B2cInvoiceLineEditor {

    /**
     * Returns the builder this overlay writes into, which is the builder the typed editor of the
     * core model writes into as well.
     *
     * @return the builder the overlay was opened over
     */
    SemanticDocument.Builder builder();

    /**
     * Returns the path of the group instance this overlay writes into, which is the path the core
     * model gives that instance.
     *
     * @return the path every value of this overlay lies under
     */
    SemanticPath path();

    /**
     * Returns the document as it has been written so far.
     *
     * @return an immutable document holding what was written into this overlay and into every other
     *     editor of the same builder, in canonical path order
     */
    SemanticDocument document();

    /**
     * BT-B2C-001 Displayed gross unit price. The unit price of the item of this invoice line with
     * VAT included, as it was shown to or agreed with the customer; the term is present only where
     * such a figure was part of the price statement, and this registry defines no arithmetic
     * relation between it and any other term.
     *
     * <p>Declared cardinality 0..1, semantic data type UnitPriceAmount. A term of ESJ-B2C 0.1,
     * which is no part of EN 16931-1.
     *
     * @param value the value to write, or {@code null} to remove the value
     * @return this editor
     */
    B2cInvoiceLineEditor displayedGrossUnitPrice(BigDecimal value);

    /**
     * BT-B2C-002 Displayed gross line total. The total of this invoice line with VAT included, as
     * it was shown to or agreed with the customer; the term is present only where such a figure was
     * part of the price statement, and this registry defines no arithmetic relation between it and
     * any other term.
     *
     * <p>Declared cardinality 0..1, semantic data type Amount. A term of ESJ-B2C 0.1, which is no
     * part of EN 16931-1.
     *
     * @param value the value to write, or {@code null} to remove the value
     * @return this editor
     */
    B2cInvoiceLineEditor displayedGrossLineTotal(BigDecimal value);

    /**
     * BT-B2C-003 Displayed line VAT amount. The VAT amount of this invoice line as it was shown to
     * the customer; the term is present only where a VAT amount was actually shown at line level,
     * and this registry defines no arithmetic relation between it and any other term.
     *
     * <p>Declared cardinality 0..1, semantic data type Amount. A term of ESJ-B2C 0.1, which is no
     * part of EN 16931-1.
     *
     * @param value the value to write, or {@code null} to remove the value
     * @return this editor
     */
    B2cInvoiceLineEditor displayedLineVatAmount(BigDecimal value);
}
