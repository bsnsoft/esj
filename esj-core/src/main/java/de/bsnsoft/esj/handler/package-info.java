/**
 * Event-driven access to a document: a handler that is told where a group instance begins
 * and ends and which values it carries, a replay that produces those events from a
 * document in canonical order, and a collector that builds a document back from them.
 */
package de.bsnsoft.esj.handler;
