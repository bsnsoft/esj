/**
 * EN16931 Semantic JSON (ESJ): a path-based JSON binding of the EN 16931-1 semantic
 * invoice model, in which every value is addressed by a semantic path built from
 * business group and business term identifiers, and the semantic data type of a value is
 * a fact of the model that the registry records once rather than a token the document
 * repeats. This package holds the core model types; the readers, writers and validators
 * live in the {@code json}, {@code model}, {@code validate} and {@code handler}
 * subpackages.
 */
package de.bsnsoft.esj;
