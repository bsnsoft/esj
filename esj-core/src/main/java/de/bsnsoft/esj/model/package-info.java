/**
 * The term registry: the machine-readable form of the structural facts of the semantic
 * model — the identifiers, the parent relation, the cardinalities, the semantic data
 * types and the supplementary components (specification, section 10). The validation
 * layers L2 and L3 are defined against it.
 */
package de.bsnsoft.esj.model;
